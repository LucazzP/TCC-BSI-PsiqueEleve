import 'package:flutter/material.dart';

class AppBorderRadius {
  static const BorderRadius extraMini = BorderRadius.all(Radius.circular(2));
  static const BorderRadius mini = BorderRadius.all(Radius.circular(4));
  static const BorderRadius tiny = BorderRadius.all(Radius.circular(8));
  static const BorderRadius small = BorderRadius.all(Radius.circular(10));
  static const BorderRadius medium = BorderRadius.all(Radius.circular(15));
  static const BorderRadius large = BorderRadius.all(Radius.circular(20));
  static const BorderRadius mega = BorderRadius.all(Radius.circular(25));
  static const BorderRadius circular = BorderRadius.all(Radius.circular(90));
}
